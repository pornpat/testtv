<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true"
        android:drawable="@color/default_background" /> <!-- pressed -->
    <item android:state_focused="true"
        android:drawable="@color/default_background" /> <!-- focused -->
    <item android:state_hovered="true"
        android:drawable="@color/default_background" /> <!-- hovered -->
    <item android:drawable="@android:color/transparent" /> <!-- default -->
</selector>